#include<iostream>

using namespace std;

int a, b, c, d;

void solve()
{
    int i, j;

    if(b > a && c > d && (b - a - (c - d) <= 1))


    for(i = 0;i <= 2;i++)
    {
        for(j = 0;j <= 2;j++)
        {
            if(i == 0 && j == 0)
                continue;
        }
    }
}

int main()
{
    cin>>a>>b>>c>>d;
    return 0;
}